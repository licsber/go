package lTime

// TimeCost copy下面的代码到需要的地方.
func TimeCost() {
	panic("Usage: Ctrl-C && Ctrl-V.")
	/*
		_startTime := time.Now()
		defer func() {
			fmt.Print("Time cost: ")
			fmt.Println(time.Now().Sub(_startTime))
		}()
	*/
}
